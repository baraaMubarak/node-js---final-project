const newsRouters = require('./newsRouters')
const profileRouters = require('./profileRouters')
const userRouters = require('./userRouters')

module.exports = {
    newsRouters,
    profileRouters,
    userRouters
}